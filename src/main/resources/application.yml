# H2 database properties
spring:
  datasource:
    url: jdbc:h2:file:./data/mydb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
      show-sql: true
  logging:
     level:
       root: DEBUG
# server:
#   port: 8079

# spring:
#   datasource:
#     url: jdbc:mysql://localhost:3306/wardrobe
#     username: izanis
#     password: password
#     platform: mysql
#   jpa:
#     hibernate:
#       ddl-auto: validate
#     database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
#     database: mysql
#     show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: guru_database_create.sql